spring:
  application:
    name: borrow

  datasource:
    url: jdbc:mysql://localhost:3306/borrow?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: borrow
    password: borrow1234
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
    open-in-view: false

springdoc:
  swagger-ui:
    path: /swagger-ui.html
